namespace VerifyOptType{

    export interface verifyOtp {
        email: string,
        otp: number
    }

    export interface ResendOtp {
        email: string
    }
}

export default VerifyOptType